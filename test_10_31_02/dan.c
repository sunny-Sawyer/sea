/*
#include<stdio.h>
#include<math.h>
int main()
{
	int i = 0;
	int p, q, r;
	for (i = 100;i <= 999;i++)
	{
		p = 0.01*i;
		q = 10*(0.01*i-p);
		r = i-100*p-10*q;
		if (i == (pow(p, 3) + pow(q, 3) + pow(r, 3)))
			printf("%d ", i);
	}
	return 0;
}*/
